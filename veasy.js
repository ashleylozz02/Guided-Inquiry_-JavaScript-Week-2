//VERY EASY: Write a function
///named min that takes
//two arguments and returns their minimum.

function min(x, y) {
  if (x <= y) {
    return x;
  } else {
    return y;
  }
}
let argument = min(3, 4);
console.log(argument);
